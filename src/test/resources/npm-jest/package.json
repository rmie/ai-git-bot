{
  "name": "mock-npm-jest",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest-junit": {
    "outputDirectory": ".",
    "outputName": "jest.xml"
  }
}
